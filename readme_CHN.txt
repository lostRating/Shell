实现的语言是c++

用while循环控制一直执行
每次读入一行指令，如果读入到结束信息，则将后台fork出的子线程都kill掉
如果读到正常指令，则对其进行分析并执行
由于我对输入输出定向不太熟悉，所以我一开始先fork了一下，确保最后能够还原回初始的输入流和输出流
这样的写法会导致cd指令比较特殊，由于.. | ..这样的指令中cd是不会对当前路径造成影响的
所以只需要考虑只有一条指令的时候，由于一开始fork的写法不太好处理这样的情况，故直接特判了一下
管道的处理是对于 A | B中形如A和B的每条指令都fork一下，子线程重定输出，父进程等待子线程结束后重定向输入
